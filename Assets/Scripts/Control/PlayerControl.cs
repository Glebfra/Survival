using UnityEngine;

namespace Control
{
    [RequireComponent(typeof(CameraControl), typeof(MovementControl), typeof(RotationControl))]
    public class PlayerControl : MonoBehaviour
    {
        
    }
}